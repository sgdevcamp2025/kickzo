import styled from 'styled-components';

export const Profile__Nickname = styled.div`
  padding: 4px;

  font-size: 18px;
  font-weight: 600;
`;

export const Profile__MyNickname = styled.input`
  padding: 2px;
  border: none;
  border-radius: 4px;
  background-color: var(--palette-line-solid-normal);
  font-size: 18px;
`;
export const Profile__MyIntroduce = styled.input`
  padding: 2px;
  border: none;
  border-radius: 4px;
  background-color: var(--palette-line-solid-normal);
  font-size: 16px;
  // font-weight: 600;
`;
